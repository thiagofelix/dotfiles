;;; config --- Package
;;; Commentary:
;;; Code:

(require 'config.ui)
(require 'config.ido)
(require 'zoom-frm)

;;Mac only
;(when (memq window-system '(mac ns))
  ;(exec-path-from-shell-initialize))

(provide 'config)
;;; config ends here
